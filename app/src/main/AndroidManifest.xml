<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rollncode.clicker">
    
    <permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    
    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        
        <provider
            android:name=".provider.ClicksProvider"
            android:authorities="com.rollncode.clicker.provider.ClicksProvider"
            android:exported="true"
            android:grantUriPermissions="true"
            android:label="clicksProvider"
            android:readPermission="com.rollncode.clicker.provider.ClicksProvider.READ"
            android:writePermission="com.rollncode.clicker.provider.ClicksProvider.WRITE"/>
        
        <activity android:name="com.rollncode.clicker.screen.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <activity android:name="com.rollncode.clicker.screen.RecordsForDayActivity">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
    </application>

</manifest>